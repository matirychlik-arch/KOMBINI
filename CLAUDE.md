# CLAUDE.md

This file provides guidance to AI assistants working in this repository.

## Repository Status

This is a newly initialized repository (`matirychlik-arch/KOMBINI`) with no application code yet. This CLAUDE.md will be updated as the project evolves.

## Git Configuration

- **Remote origin**: `http://local_proxy@127.0.0.1:36927/git/matirychlik-arch/KOMBINI`
- **Git user**: Claude (noreply@anthropic.com)
- **GPG signing**: Enabled with SSH key

### Branch Naming Convention

AI-generated branches follow the pattern:
```
claude/<task-slug>-<session-id>
```

### Git Workflow

1. Always develop on the designated feature branch
2. Never push directly to `main` without explicit permission
3. Use descriptive commit messages
4. Push with: `git push -u origin <branch-name>`

## Development Setup

> **Note**: No application code exists yet. Update this section once the project is initialized.

### Getting Started

```bash
# Clone the repository
git clone http://local_proxy@127.0.0.1:36927/git/matirychlik-arch/KOMBINI
cd KOMBINI

# (Add setup instructions here once the project is initialized)
```

## Project Structure

> To be filled in once the project is initialized.

```
KOMBINI/
├── CLAUDE.md        # This file
└── (project files will go here)
```

## Technology Stack

> To be determined once the project is initialized.

## Key Conventions

> To be established once the project is initialized.

### Code Style

- (Define language/framework-specific style guidelines here)

### Testing

- (Define testing conventions, commands, and requirements here)

### Environment Variables

- (Document required environment variables here)

## Commands

> To be added once the project is initialized.

```bash
# Common commands will be listed here, e.g.:
# npm install      - Install dependencies
# npm test         - Run tests
# npm run build    - Build the project
# npm run dev      - Start development server
```

## Notes for AI Assistants

- This repository is empty — when asked to implement features, create appropriate project structure first
- Always read existing files before modifying them
- Keep changes minimal and focused on what was requested
- Avoid over-engineering; prefer simple, direct solutions
- Update this CLAUDE.md whenever significant project structure or conventions are established
